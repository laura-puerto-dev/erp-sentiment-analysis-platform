#!/bin/sh

echo "Running pre-commit quality gate..."

./mvnw verify
status=$?

if [ $status -ne 0 ]; then
    echo "Quality gate failed. Commit aborted."
    exit $status
fi

echo "Quality gate passed."
